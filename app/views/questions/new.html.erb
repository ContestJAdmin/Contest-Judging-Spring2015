<%= button_to 'Back', 'javascript:history.go(-1);' %>
<h1>
    Add New Question
</h1>
<div>
<%= form_for @question, url: contest_question_type_questions_path(@question_type.contest_id, @question_type) do |f| %>
    <%= render 'fields', :f => f %>
    <%= f.submit "Create"%>
<% end %>
</div>

    
    
   