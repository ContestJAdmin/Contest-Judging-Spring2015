<%= button_to 'Back', 'javascript:history.go(-1);' %>
<h1>
    Add New Project
</h1>
<div>
<%= form_for([@contest, @project]) do |f| %>
    <%= render 'fields', :f => f %>
    <%= f.submit "Create"%>
<% end %>
</div>
